type Query {
    buyers(nameFilter: String): [Buyer]
}

type Product @key(fields: "buyerId") @extends {
    buyerId: Int @external
    buyer: Buyer
}

type Buyer @key(fields: "id") @key(fields: "characterId") {
    id: Int!
    name: String!
    characterId: String
}
